import { FormatageCCVDirective } from './formatage-ccv.directive';

describe('FormatageCCVDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatageCCVDirective();
    expect(directive).toBeTruthy();
  });
});
