import { FormatageDateDirective } from './formatage-date.directive';

describe('FormatageDateDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatageDateDirective();
    expect(directive).toBeTruthy();
  });
});
