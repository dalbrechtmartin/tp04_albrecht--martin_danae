import { FormatageCodeDirective } from './formatage-code.directive';

describe('FormatageCodeDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatageCodeDirective();
    expect(directive).toBeTruthy();
  });
});
